{
  "name": "express-batch",
  "version": "0.2.1",
  "description": "Middleware for Express that allows for batched API requests.",
  "main": "express-batch.js",
  "scripts": {
    "test": "node_modules/.bin/mocha  --reporter spec --bail --growl --check-leaks test/",
    "test-cov": "node_modules/.bin/istanbul cover node_modules/.bin/_mocha -- --reporter dot --check-leaks test/",
    "test-travis": "node_modules/.bin/_mocha --reporter spec --check-leaks test/",
    "test-travis-cov": "node_modules/.bin/istanbul cover node_modules/.bin/_mocha --report lcovonly -- --reporter spec --check-leaks test/"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/yarikos/express-batch.git"
  },
  "keywords": [
    "express",
    "expressjs",
    "batch",
    "api"
  ],
  "author": "https://github.com/yarikos",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/yarikos/express-batch/issues"
  },
  "dependencies": {},
  "devDependencies": {
    "express": "4.*",
    "mocha": "2.x",
    "should": "^7.1.1",
    "supertest": "^1.1.0"
  }
}
