{
  "name": "express-batch",
  "version": "0.0.6",
  "description": "Middleware, which allow you to perform batch requests to your express.js powered API.",
  "main": "express-batch.js",
  "scripts": {
    "test": "node_modules/.bin/mocha  --reporter spec --bail --growl --check-leaks test/",
    "test-cov": "node_modules/.bin/istanbul cover node_modules/.bin/mocha -- --reporter dot --check-leaks test/",
    "test-travis": "node_modules/.bin/istanbul cover node_modules/.bin/mocha --report lcovonly -- --reporter spec --check-leaks test/"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/yarikos/express-batch.git"
  },
  "keywords": [
    "express",
    "expressjs",
    "batch",
    "api"
  ],
  "author": "https://github.com/yarikos",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/yarikos/express-batch/issues"
  },
  "dependencies": {},
  "devDependencies": {
    "express": "4.x",
    "istanbul": "^0.3.2",
    "mocha": "2.x",
    "should": "4.x",
    "supertest": "0.15"
  }
}
